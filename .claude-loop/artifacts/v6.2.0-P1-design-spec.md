# v6.2.0 P1 設計規格（精簡閉環步驟 1 · v2 — 採納 P1b 修正）

> 日期：2026-04-26
> Branch：feature/v6.0.0-karpathy（沿用 v6.x 系列分支）
> 上游：dev-closed-loop/design/09-v6.2.0-cognitive-kpi.md（brainstorming 已 commit b24d7d3）
> 用戶決策：K-14 落點 = 擴 Section 12.5 第 5 條為協議 / K-11 套餐 = 精簡 3 指標 + 三區間門檻 / 執行順序 = K-14 → K-10 → K-11 / 閉環模式 = 精簡六步
> Scope：v6.2.0 milestone（3 條 K-x：K-14 / K-10 / K-11）

---

## 0 學習查詢結果

| 來源 | 結果 |
|------|------|
| 問題追蹤長期警惕模式（5 條） | 命中 [#003] 單線索→事實（K-14 設計上要避免「我推一次就斷言觸發 4 條已涵蓋全部場景」）/ [#001 #002 #004 #005] 不適用（無 existence claim、無 routing、無 config 字面 / 共用值場景）|
| `.claude-loop/learning-log.md`（v6.0.0 + v6.1.0 共 5 條） | 命中 v6.0.0 事件 4 條 + v6.1.0 milestone closure 觀察項 2 條（DR 修正傳遞性 / 行數預算膨脹週期）|

**關鍵教訓帶過來**：

- **#003 警惕**：K-14 第 5 條「5.1 觸發場景必須全部成立才觸發」這 4 條是我設計推論而非外部依據——需要 design-reviewer 在步驟 1b 挑戰是否有遺漏的觸發情境（例：用戶斷言形式不是 explicit「X 是 Y」但仍是事實宣告——「X 在那裡」「X 跑著」這類隱式陳述）
- **DR 修正傳遞性**（v6.0.0 觀察項 1）：3 條 K-x 跨多檔案，DR 修正一處時要 walk 全部 BC list（v6.0.0 教訓：DR-1 修正 anchors 升級為 list of objects → BC-4 行數從 25 變 30，當時沒同步調整 BC list 預算）
- **行數預算膨脹週期**（v6.0.0 觀察項 2）：v6.1.0 +10 行（520），v6.2.0 預算控制在 +25 行內（545 上限），緩衝 ≥ 5 行避免單行誤差觸發 R-x medium

**Step 0a 字面證據掃描**：不適用（無 config / 無未知檔案推論）
**Step 0b 共用值檢測**：不適用
**Step 0c 模組資產查詢**：本 repo 無 `module-registry.md`
**v6.0.0 教訓 #1（部署生命週期視角檢查）**：本次 K-14 / K-10 / K-11 改動不涉及 metadata 中含 placeholder 的 grep 字串 → **不適用**（沿用 v6.0.0 教訓 #1 已處理的對象範疇）

---

## 1 分層結構聲明

純文檔升級。本次動方法論文檔層三類：
- CLAUDE_TEMPLATE.md 主檔（K-14 主要落點）
- `.claudedocs/standards/`（K-10 對應表 + K-14 Push back 變體）
- `.claudedocs/concepts/`（K-14 對稱表 + K-11 新檔案）

不動 README 對外層、不動 Skill 部署層、不動 setup.sh 安裝邏輯（除非依賴影響表觸發）。

---

## 2 邊界條件（BC-x）

### BC-1 [testable]：Section 12.5 第 5 條 → 完整事實質疑協議（K-14）

**位置**：
1. `CLAUDE_TEMPLATE.md` Section 12.5（第 5 條從一行擴為 5.1 / 5.2 / 5.3 / 5.4 子段）
2. `.claudedocs/standards/產出物格式.md` line 675-688「Push back 輸出格式」section 加事實質疑變體
3. `.claudedocs/concepts/閉環核心理念.md`「主動質疑」段補對稱表（Claude→自己 / Claude→用戶 / 用戶→Claude）

**內容**（CLAUDE_TEMPLATE 第 5 條展開草案）：

```markdown
5. **用戶事實前提待驗證**：用戶斷言為「事實」但無證據的關鍵主張，且該主張將作後續行動前提
   （與 Section 12 對 Claude 自身推論的對稱面 · v6.2.0 K-14 完整協議）

   #### 5.1 觸發場景（4 條必須全部成立才觸發）
   - 用戶斷言形式為「X 是 Y」/「X 等於 Y」/「X 在 Y 位置」/「X 在跑」/「X 已部署」等事實陳述（含隱式）
   - 該斷言**未附證據**（檔:行 / 截圖 / 系統輸出 / 既有 memory 紀錄）
   - 該斷言將作為後續行動前提（修改 / SSH / DB / 部署 / 寫 memory）
   - 若該斷言為假，後續行動代價非微小（rollback / 資料污染 / 路徑錯誤）

   #### 5.2 反向質疑輸出格式（觸發即輸出）

   ⚠️ 我需要先驗證這個前提
   ├─ 用戶斷言：[X 是 Y]
   ├─ 證據要求：A 級 ≥ 1（檔:行）/ 或 B 級 + 反例通過 / 或 「請補證據」
   ├─ 我能查到的：[Claude 自查結果，含查找位置 + 結論]
   ├─ 反例檢查：若主張為真應觀察到 X / 為假應觀察到 Y / 實際 Z
   └─ 處置：請補證據 / 確認推論等級（強/中/弱）/ OK 用原方案

   #### 5.3 與 Section 12 的對稱性

   | 觸發來源 | 機制 | 主動方 | 輸出位置 |
   |---------|------|--------|---------|
   | Claude 自身推論 → 事實 | Section 12 事實主張閘門 | Claude 自查 | inline 對話或 P5 verifier |
   | 用戶斷言 → 行動前提 | Section 12.5 第 5 條 | Claude 反問用戶 | inline 對話 |

   #### 5.4 解除條件（按事實質疑代價結構分支處理 · DR-4 修正）
   - 用戶補充 A 級證據（檔:行 / 系統輸出 / 截圖）→ 立即解除（事實已驗證）
   - 用戶確認 B 級 + 反例通過 → 立即解除（事實有合理依據）
   - 用戶說「中度推論可接受」→ 解除執行；Claude inline 標注「⚠️ 基於未驗證前提執行，事實若錯誤代價：[列舉具體後果]」
   - 用戶說「OK 用原方案」但不補證據、不確認推論等級 → **不自動解除**；要求 explicit 確認「中度推論可接受，承擔事實錯誤代價」

   **與 1-4 條的差異**：1-4 條代價可量（方案次優，已知），5 條代價不可量（事實錯誤導致 rollback / memory 污染 / 路徑錯誤，未知）。一句「OK」不能跨越 1-4 條 vs 5 條的代價結構差異。
```

**concepts/閉環核心理念.md「主動質疑」段 — 3-row 對稱表草案**（DR-3 補）：

```markdown
| 觸發方向 | 機制 | 主動方 |
|---------|------|--------|
| Claude 對自己推論升格事實 | Section 12 事實主張閘門 | Claude 自查 |
| Claude 對用戶斷言事實 | Section 12.5 第 5 條（v6.2.0 K-14） | Claude 反問用戶 |
| 用戶對 Claude 結論 | Section 13 質疑熔斷協議 | 用戶質疑（白名單句式） |

（純表 + 一行標題語，prose 補充已在 CLAUDE_TEMPLATE 5.3 含；concepts 此段純表展示三方關係）
```

**standards/產出物格式.md「Push back 輸出格式」section 重組**（DR-7 修正）：

```markdown
## Push back 輸出格式

### 變體 1：1-4 條場景（方案爭議 · v6.0.0 既有）
[既有 3 分支樹狀：理由 / 替代方案 / 若仍要執行原方案]

### 變體 2：第 5 條場景（事實前提質疑 · v6.2.0 K-14 新增）
[新增 5 分支樹狀：用戶斷言 / 證據要求 / 我能查到的 / 反例檢查 / 處置]

### 何時用哪個變體
- 觸發場景在 Section 12.5 第 1-4 條 → 變體 1
- 觸發場景在 Section 12.5 第 5 條（5.1 全部成立）→ 變體 2
```

**行數限制**（v2 修正：DR-1 觸發 concepts 增量降低）：
- CLAUDE_TEMPLATE 第 5 條展開：≤ 25 行（含 5.4 分支處理 4 條）
- standards 變體段：≤ 15 行（含「何時用哪個變體」說明，3 段重組）
- concepts 對稱表段：≤ 8 行（純 3-row 表 + 一行標題語，不寫 prose）
- 三檔合計：≤ 48 行

**驗收**（4 子條目 BC-K14-x 全部命中）：
- BC-K14-1：CLAUDE_TEMPLATE Section 12.5 第 5 條含 5.1 / 5.2 / 5.3 / **5.4 分支處理 4 條** 四子段標題
- BC-K14-2：產出物格式.md「Push back 輸出格式」section 含 **變體 1 / 變體 2 / 何時用哪個** 三段（重組）
- BC-K14-3：閉環核心理念.md「主動質疑」段含 **3-row 對稱表**（觸發方向 = Claude→自己 / Claude→用戶 / 用戶→Claude）
- BC-K14-4：CLAUDE_TEMPLATE 內「將擴充」字串（line 271 / 284）改為「已擴充」

---

### BC-2 [testable]：ID ↔ 失誤類型對應表（K-10）

**位置**：`.claudedocs/standards/產出物格式.md` 開頭新增 section（既有 line 19 「---」分隔線之前、line 11「格式裡的 ID 系統」段之後）

**內容**：

```markdown
## ID ↔ 失誤類型對應表（v6.2.0 K-10 新增）

> 每種 ID 出問題時最常對應到哪類 failure_type — 用於 learning-log 條目的 failure_type 快速判定。

| ID 系統 | 用途 | 常見 failure_type | 邊界情境（次要可能） |
|---------|------|-------------------|---------------------|
| BC-x（邊界條件） | 設計規格定義行為邊界 | judgment_failure（漏設計 / 設計錯誤） | verification_failure（設計到了但測試漏蓋） |
| EH-x（錯誤處理） | 設計規格定義異常處理 | judgment_failure（漏設計 / 處理策略錯） | verification_failure（測試覆蓋不足） |
| R-x（檢核發現） | Phase 3 程式碼問題 | verification_failure（reviewer 漏抓 → P5 才發現） | judgment_failure（reviewer 抓但建議錯方向） |
| R-style（風格漂移） | v6.1.0 R-x 子類 | verification_failure | tooling_failure（lint 工具誤判） |
| DR-x（設計審查） | Phase 1b 設計缺陷 | judgment_failure（架構選錯方向） | process_failure（design-reviewer 跳步驟） |
| IF-x（介面契約） | 跨模組 API 契約 | judgment_failure（契約設計失準） | process_failure（CR-x 流程沒跑） |
| CR-x（契約變更） | 介面變更管理 | process_failure（變更沒走 CR 流程） | judgment_failure（變更影響評估錯） |

**用法**：
- learning-log 寫 failure_type 時先查此表，符合常見對應 → 直接填；屬邊界情境 → 在「教訓」欄附一句邊界判定理由
- 此表不窮舉 — tooling_failure（hook bug / script 誤判）跨所有 ID 種類，獨立判斷不查表
- v6.2.0 K-14 擴充 Section 12.5 第 5 條後，「事實誤判」事件條目（v5.22.3 引入）的 failure_type 判定不變（judgment_failure 為主，verification_failure 為次）
```

**行數限制**：≤ 18 行（v2 修正：DR-6 採方案 A 保留現狀但實作壓 ≤ 18 行，從原 25 行降下，間接幫 standards 緩衝多 7 行）

**驗收**（3 子條目 BC-K10-x 全部命中）：
- BC-K10-1：產出物格式.md 開頭存在「## ID ↔ 失誤類型對應表」section
- BC-K10-2：表格 7 列（BC/EH/R/R-style/DR/IF/CR）× 4 欄（ID 系統/用途/常見 failure_type/邊界情境）齊全
- BC-K10-3：附「用法」條列 ≥ 3 點（含 v5.22.3「事實誤判」對齊聲明）

---

### BC-3 [testable]：方法論運作指標 KPI 新檔（K-11）

**位置**：新增 `.claudedocs/concepts/方法論運作指標.md`

**內容**：

```markdown
# 方法論運作指標

> v6.2.0 K-11 引入。給閉環方法論本身設可觀測的健康指標——回答「方法論在這專案上是否在 working」。

## 為什麼需要 KPI

閉環方法論有大量規則 / 閘門 / 子 agent / 回退邏輯——但「跑了一年下來，這套到底有用嗎」並沒有可觀測指標。光憑體感容易陷入 confirmation bias（規則用熟了就覺得有效）。

KPI 不是用來「達標領獎」，是用來**捕捉走偏的早期信號**：當數字朝一個方向飄，就是回頭看方法論是否需調整的訊號。

## 三指標套餐（產出物層）

| 維度 | 指標 | 來源 | 時間尺度 |
|------|------|------|---------|
| 設計品質 | 每閉環 DR-x high 數 | Phase 1b 報告（`.claude-loop/artifacts/P1b-design-review.md`） | 短期（per 閉環）|
| 程式品質 | 每閉環 R-x high 數 | Phase 3 報告（`.claude-loop/artifacts/P3-quality-review.md`） | 短期（per 閉環）|
| 學習效力 | 升格觸發次數 / 月 | learning-log → 問題追蹤的升格條目 | 中期（per 月）|

長期方向（per 季）：上述 3 指標 3 個月滑動平均的趨勢——是否朝健康區間收斂。

## 三區間門檻

| 指標 | 健康 🟢 | 警示 🟡 | 危險 🔴 |
|------|--------|--------|--------|
| DR-x high 數 | ≤ 1 | 2-3 | ≥ 4 |
| R-x high 數 | 0 | 1 | ≥ 2 |
| 升格觸發 / 月 | 1-3 | 0 或 4-5 | ≥ 6 |

**關鍵設計**：升格 = 0 並非越低越好。
- 過低（0）→ 「沒在學」訊號（失誤事件未升格 = learning-log 流失）
- 過高（≥ 6）→ 同類問題反覆出現未根治 → 方法論可能存在系統性缺陷

## 走偏處置流程

### 短期走偏（單一閉環觸發）
- 🟡 警示 → 在 Phase 5 學習日誌補一段「為什麼這次 DR-x / R-x 較高」
- 🔴 危險 → 強制觸發讀書會（用戶 + Claude 一起檢視該閉環的 P1 / P3 報告，找根因）

### 中期走偏（連續 2 個月）
- 🟡 警示 → 寫 design 條目記錄走偏歷程
- 🔴 危險 → 觸發 minor / major 升級規劃，重新檢視方法論本身

## 為什麼不採完整 9 指標

完整套餐含「行數變動率 / 配額降級 / 雙向追溯命中率」等 6 個進階指標，覆蓋面廣但維護成本高。三指標已足夠捕捉「設計品質 / 程式品質 / 學習效力」三大維度，邊際資訊增益有限——遵循 Karpathy Simplicity 原則。

完整套餐保留為「方法論演進到 v7+ 後重新評估基線」的選項。

## 觀察期與基線校準

v6.2.0 ~ v6.3.x 為觀察期，記錄三指標數值但門檻可微調：
- 累積 ≥ 5 個閉環後評估 DR-x / R-x 區間是否合理
- 累積 ≥ 6 個月後評估升格頻率區間是否合理
- 校準後在 v7 等大版本固化門檻
```

**行數限制**：新檔案 80-120 行（含 heading）

**驗收**（5 子條目 BC-K11-x 全部命中）：
- BC-K11-1：新檔案 `concepts/方法論運作指標.md` 存在且 section 結構完整（為什麼需要 / 三指標 / 三區間 / 走偏處置 / 觀察期）
- BC-K11-2：三指標表 3 列（DR-x high / R-x high / 升格觸發），每列「來源」與「時間尺度」欄齊全
- BC-K11-3：三區間表數值跟用戶核可一致（DR-x：≤1/2-3/≥4；R-x：0/1/≥2；升格：1-3/0或4-5/≥6）
- BC-K11-4：「升格 = 0 並非越低越好」設計精神有獨立段闡明
- BC-K11-5：時間尺度標注（短期 / 中期 / 長期方向）explicit

---

### BC-4 [testable]：依賴影響表全部連動檔案同步（跨 K-x）

**位置**：依 design/09 line 152-159 列出的 4 處依賴影響鏈

**內容**：v6.2.0 改完三條 K-x 後，必須同步：
1. CLAUDE_TEMPLATE.md 末尾 closed-loop 註解：v6.1.0 → v6.2.0
2. CLAUDE_TEMPLATE.md 末尾 migration-notes 區塊新增 `from-version: v6.0/v6.1` `to-version: v6.2.0` 條目
3. `dev-closed-loop/README.md` 版本歷史增 v6.2.0 條目
4. 根 `README.md` 版本歷史增 v6.2.0 條目
5. `.claudedocs/README.md` 核心檔案清單從 10 → 11 份 concept 檔案（含方法論運作指標.md）
6. `setup.sh` 驗證清單若有 hardcode `.claudedocs/concepts/` 檔案數需更新（步驟 2 前 grep 確認）
7. `dev-closed-loop/skill/init-claude.md` 部署清單若有 hardcode 需更新

**行數限制**：版本號 + migration-notes 合計 ≤ 8 行

**驗收**（v2 修正：DR-5 兩段式驗收）：

```
必同步（5 處）：CLAUDE_TEMPLATE.md 末尾 / migration-notes / dev-closed-loop/README.md / 根 README.md / .claudedocs/README.md
  → grep `v6.2.0` 在這 5 處各 ≥ 1 命中

條件式（2 處）：setup.sh / dev-closed-loop/skill/init-claude.md
  → 步驟 2 前先 grep `.claudedocs/concepts/` hardcode 數
  → 命中 hardcode → 同步更新並驗 grep `v6.2.0` ≥ 1 命中
  → 未命中 hardcode → 標 N/A（不算驗收失敗）
```

---

## 3 錯誤處理（EH-x）

純文檔升級，無外部 I/O，**聲明跳過 EH-x**（沿用 v6.1.0 慣例）。

唯一例外：`setup.sh` 部署邏輯若發現 hardcode 檔案數錯誤，視同程式碼問題處理（屬 R-x 範疇而非 EH-x）。

---

## 4 介面契約（IF-x / CR-x）

無模組間介面變更（純文檔內部修改）。CLAUDE_TEMPLATE Section 12.5 第 5 條展開**不算 IF**——它只是現有 Section 內部展開，沒新增對外介面。

`.claudedocs/concepts/方法論運作指標.md` 是新檔但沒有「對外介面」（concept 檔案是給人類閱讀，不被其他檔案 import）。

---

## 5 設計決策

| 決策 | 選擇 | 排除選項 | 理由 |
|------|------|---------|------|
| K-14 落點 | Section 12.5 第 5 條展開為協議 | 擴 Section 12 / 新增 12.6 / 新增 14 | v6.0.0 已預留接續點；不增 section；跟 push back 機制連續（用戶 brainstorming 決策）|
| K-11 套餐 | 精簡 3 指標 | 6 指標 / 9 指標 / 1 指標 | 邊際資訊增益遞減；遵循 Karpathy Simplicity；維護成本最低（用戶 brainstorming 決策）|
| K-11 門檻 | 三區間（健康/警示/危險） | 硬閾值 / 方向性 / 不設 | 軟性導引避免無基線下硬閾值誤判（用戶 brainstorming 決策）|
| 執行順序 | K-14 → K-10 → K-11 | 任意 | K-14 確認後 K-10 範圍鎖定；K-11 獨立可後做 |
| 閉環模式 | 精簡六步 | 完整 5-Phase | minor 升級 v6.1.0 已驗證模板（0 失敗事件、cost 低 ~70%）|

---

## 6 行數預算（v2 修正：DR-1 觸發 concepts 增量降低）

| 檔案 | v6.1.0 | v6.2.0 上限 | 增量上限 | 緩衝 |
|------|--------|------------|---------|------|
| CLAUDE_TEMPLATE.md | 520 | ≤ 545 | +25（K-14 第 5 條 5.1-5.4 展開 + migration-notes 條目）| 5 行 ✅ |
| standards/產出物格式.md | 691 | ≤ 730 | +33（K-10 表 ≤ 18 / K-14 變體段 ≤ 15 / 版本標注 +2 → 但 K-10 從 25 壓到 18 省了 7 行）| 6 行 ✅ |
| concepts/閉環核心理念.md | 229 | ≤ 245 | +6（K-14 對稱表純 8 行 + 既有「主動質疑」段瘦身 ≥ 5 行騰出空間，凈增 ≤ 6）| 10 行 ✅ |
| concepts/方法論運作指標.md（新） | — | 80-120 | 新檔 80-120 | 40 行 buffer ✅ |
| 跨檔合計 | — | — | +144 ~ +184 行 | — |

**緩衝判定**（沿用 v6.0.0 教訓 #2「≥ 5 行」）：四檔均 ≥ 5 行 ✅。

**concepts 瘦身位置（DR-1 方案 A）**：實作步驟 2 前，主 agent grep 既有 `concepts/閉環核心理念.md` 找：(a) v6.0.0 / v6.1.0 末尾「最後修訂」與版本歷史的重複描述；(b)「閉環原則」段或「逐層擴展」段中的冗餘比喻。瘦身 ≥ 5 行後再加 K-14 對稱表，確保緩衝 ≥ 5 行硬規則。

---

## 7 設計自檢（7 問 · v6.1.0 K-02 加入第 7 問）

| # | 自檢題 | 答 |
|---|-------|-----|
| ① | 更簡單的做法？ | brainstorming 已比較 4 形態 × 4 套餐，選最簡。K-11 用 3 指標而非 6/9 |
| ② | 影響不相關模組？ | 不影響 setup.sh 安裝邏輯（除非 hardcode 檔案數，已列入 BC-4 風險）；不影響 Skill 部署層 |
| ③ | 邊界條件覆蓋？ | BC-K14-x 4 條 / BC-K10-x 3 條 / BC-K11-x 5 條 / BC-4 跨檔同步 1 條 = 13 條 |
| ④ | 操作流程變複雜？ | K-14 第 5 條從一行擴 4 子段（+25 行）但結構清晰；K-10 是查找表降低判讀成本；K-11 是觀察指標不增加流程義務 |
| ⑤ | 解法複雜度成正比？ | 3 條 K-x 對應 3 個 v6.0.0/v5.22.2 既有機制的延伸，無發明新概念 |
| ⑥ | 架構地基穩？ | Section 12.5 已存在（v6.0.0）/ 4 類 failure_type 已存在（v5.22.2）/ KPI 是新檔不影響現有閘門 |
| ⑦ | 資深工程師看到會說過度設計嗎？（K-02 senior engineer test）| **有風險**：K-11 KPI 機制可能被質疑「方法論的方法論」過度設計。緩解：用戶選最簡 3 指標（已是最節制版）；獨立檔案可在不影響閉環下被忽略；觀察期門檻可微調。**[由 P1b DR-2 arch-risk 升格追蹤至 Phase 5 觀察項，對應 RISK-9]** |

**第 ⑦ 問風險評估**：K-11 是潛在「過度設計」嫌疑點。但因為：(a) 是獨立 concept 檔案，沒進 CLAUDE_TEMPLATE 主檔強制流程；(b) 三指標都是既有產出物的事後統計，無新增工具；(c) 觀察期 explicit 標明可微調 → 不阻擋 P1。design-reviewer 步驟 1b 已標 DR-2 arch-risk（K-11 KPI 量測無強制義務）→ Phase 5 milestone 觀察項追蹤。

---

## 8 風險清單（沿用 design/09 RISK-x，由 reviewer 重審）

| ID | 風險 | 嚴重度 | 緩解 |
|----|------|--------|------|
| RISK-1 | K-14 第 5 條 5.1 觸發 4 條 AND 過嚴 → 真該 push back 場景被吃掉 | 中 | 5.1 已含「隱式陳述」例（X 在跑 / X 已部署）；觀察 ≥ 5 觸發機會評估誤判率 |
| RISK-2 | K-11 三區間門檻無歷史基線 → 初期判讀失準 | 中 | 觀察期 v6.2.0 ~ v6.3.x explicit；v7 固化 |
| RISK-3 | K-10 對應表「常見 failure_type」誤導使用者強行套用 | 低 | 「邊界情境」並列 + 附說明強制邊界判定理由 |
| RISK-4 | setup.sh / init-claude.md hardcode 檔案數 → 部署驗證失敗 | 低 | BC-4 列入 + 步驟 2 前 grep 確認（先做這一檢查再動 K-11）|
| RISK-5 | CLAUDE_TEMPLATE 行數逼近上限（520 → 545，緩衝 5 行）| 低 | K-14 第 5 條展開 ≤ 25 行嚴守；超出 → R-x medium 預算審查 |
| RISK-6 | K-11 KPI 沒人量 → 變裝飾文檔 | 中 | 三指標都是 Phase 1b/3/5 既有產出（無新工具負擔），DR-x / R-x / 升格皆可 grep 統計 |
| ~~RISK-7~~ | ~~concepts/閉環核心理念.md 行數緩衝為 0~~ | — | **已升格 P1b DR-1 high 並在 P1 v2 處理**：對稱表純 8 行 + 既有段瘦身 ≥ 5 行 → 緩衝拉到 10 行 |
| RISK-8（新 · DR-4 對應） | 5.4 解除條件「OK 用原方案」直接等同事實質疑解除 → 用戶一句 OK 繞過 5.1 觸發場景把關 | 中 | P1 v2 已改 5.4 為分支處理：「OK 用原方案」但不補證據 → 不自動解除；需 explicit 確認「中度推論可接受，承擔事實錯誤代價」。代價非對等不能單靠一句 OK 跨越 |
| RISK-9（新 · DR-2 arch-risk 對應） | K-11 KPI 制定後沒人去量 → 變裝飾性文檔（長期觀察項） | arch-risk | 採保守方案：v6.2.0 不加強制觸發點；改在 v6.3.0 規劃明列「累積三指標 ≥ 5 個閉環的數據」作為下一個 milestone 的 success criteria。Phase 5 步驟 4.5 反向追溯時記入觀察項，累積 3 期未量 → 升格為長期警惕模式 |

---

## 9 學習查詢結果（最終標示）

**學習查詢**：問題追蹤命中 [#003]（K-14 設計需避免單線索推論誤入）/ learning-log 命中 v6.0.0 4 條事件 + 2 觀察項

---

最後修訂：2026-04-26（v6.2.0 P1 v2 — P1b 採納 DR-1 high 修正 / DR-3-DR-7 medium 全採 / DR-2 arch-risk 入 RISK-9 / 緩衝全部 ≥ 5 行 ✅，進步驟 2 實作）
