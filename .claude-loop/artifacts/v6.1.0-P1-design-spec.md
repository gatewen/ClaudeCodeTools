# v6.1.0 P1 設計規格（精簡閉環步驟 1）

> 日期：2026-04-26
> Branch：feature/v6.0.0-karpathy（沿用 v6.0.0 分支，Q3=A）
> 上游：design/08-v6.0.0-karpathy-integration.md v6.1.0 milestone + 用戶 Q1=A / Q2=A 對齊
> 用戶決策：K-05 採 Karpathy「mention but don't delete」/ K-06 R-style 獨立子類別
> Scope：v6.1.0 milestone（6 條 K-x：K-02 / K-03 / K-05 / K-06 / K-08 / K-16）

---

## 0 學習查詢結果

| 來源 | 結果 |
|------|------|
| 問題追蹤長期警惕模式 | 命中 [#003]（沿用 v6.0.0 緩解：「Karpathy 原文 vs 延伸」分類已守）/ [#004, #005] 不適用（純文檔升級無 config） |
| `.claude-loop/learning-log.md` | 命中 4 條 v6.0.0 事件 + 2 觀察項（DR 修正傳遞性 / 行數預算膨脹週期）|

**關鍵教訓帶過來**：
- **DR 修正傳遞性**（v6.0.0 觀察項 1）：修正一處時 walk 全部 BC list 看是否需同步調整其他 BC 的預算/介面 → v6.1.0 P1 已主動排查（見下方 EH-2）
- **行數預算膨脹週期**（v6.0.0 觀察項 2）：major 升級必須含 ≥ 1 個瘦身條目；v6.1.0 是 minor 升級不強制瘦身但須明標預算上限

**Step 0a 字面證據掃描**：不適用（無 config / 無未知檔案推論）
**Step 0b 共用值檢測**：不適用
**Step 0c 模組資產查詢**：本 repo 無 `module-registry.md`

---

## 1 分層結構聲明

純文檔升級（同 v6.0.0 三層結構）。本次只動方法論文檔層（CLAUDE_TEMPLATE / .claudedocs/agents / .claudedocs/standards / .claudedocs/concepts），不動 README 對外層、不動 Skill 部署層。

---

## 2 邊界條件（BC-x）

### BC-1 [testable]：Senior engineer test 加進設計自檢（K-02）

**位置**：
1. `CLAUDE_TEMPLATE.md` 精簡閉環步驟 1 設計自檢段：6 問 → 7 問
2. `.claudedocs/agents/architect.md` Step 8 閘門加同一條檢核項
3. `.claudedocs/agents/design-reviewer.md` 步驟 2 挑戰式審查補 senior check 條目

**內容**（CLAUDE_TEMPLATE 步驟 1 設計自檢段）：
- 既有 6 問「① 更簡單做法 ② 影響不相關模組 ③ 邊界條件覆蓋 ④ 操作流程變複雜 ⑤ 解法複雜度成正比 ⑥ 架構地基穩」
- 補第 7 問：「**⑦ 一位資深工程師看到這個設計，會說「過度設計」嗎？**」
- 任一不確定 → AskUserQuestion

**行數限制**：≤ 5 行（三檔合計）

**驗收**：grep `資深工程師` 在 CLAUDE_TEMPLATE / architect.md / design-reviewer.md 各 ≥ 1 命中

---

### BC-2 [testable]：3x rule 重寫觸發（K-03）

**位置**：`.claudedocs/agents/architect.md` Step 1 補實作行數估算 + 加「強制重寫條件（3x rule）」段

**內容**：
1. Step 1 實作計畫補：「**估計每個檔案的實作行數**（同領域典型實作為基準，作為 P2 implementer 的預期）」
2. 新增段：
   ```
   ## 強制重寫條件（3x rule · v6.1.0 新增 · Karpathy 學習）

   若實作行數 ≥ 「同領域典型實作」的 3 倍，且不能用業務複雜度合理化：
   → 不再給優化建議，改為「重寫提案」回報主 agent
   → 主 agent 用 AskUserQuestion 讓用戶決定：採用重寫版 / 保留原版 / 中間方案

   「同領域典型實作」由 Phase 1 architect 估算（見 Step 1）。
   ```

**行數限制**：≤ 15 行

**驗收**：grep `3x rule` 或 `強制重寫條件` 在 architect.md ≥ 1 命中

---

### BC-3 [testable]：Dead Code 處理立場 — 採 Karpathy A（K-05，Q1=A）

**位置**：
1. `CLAUDE_TEMPLATE.md` 新增 Section 11.5「Dead Code 處理立場」（位於 Section 11 同類掃描之後、Section 12 事實主張閘門之前）
2. `.claudedocs/agents/code-reviewer.md` 加註：pre-existing dead code 不算 R-x 缺失

**內容**（CLAUDE_TEMPLATE Section 11.5）：
```
### 11.5 Dead Code 處理立場（v6.1.0 新增 · Karpathy 學習）

> **性質**（DR-1 修正）：非執行期硬約束（不像 Section 9 閘門有 Hook 阻擋），是修改判斷立場——由 reviewer / 主 agent 自律遵守。

- 你的改動造成的 orphan（imports/變數/函式不再被引用）→ **刪除**
- 改動前已存在的 dead code → **提及，不動**（最後彙整時告訴用戶「我注意到 X 是 dead code 但未刪」）
- 用戶明確要求清理 → 才動

**理由**：保留 pre-existing dead code 跟 Karpathy Surgical 精神一致——你的 PR 應該只解用戶的需求，不順便 refactor。
```

**行數限制**：≤ 12 行（含 heading）

**驗收**：grep `11.5 Dead Code` 在 CLAUDE_TEMPLATE 命中 1 次 + code-reviewer.md 含對應規則

---

### BC-4 [testable]：R-style 獨立 R-x 子類別（K-06，Q2=A）

**位置**：
1. `.claudedocs/agents/code-reviewer.md` 加 R-style 子類別檢核段
2. `.claudedocs/standards/產出物格式.md` R-x 編號表 / 嚴重度系統補 R-style 行

**內容**（code-reviewer.md）：
```
## R-style：Style Consistency 檢核（v6.1.0 新增 · 獨立子類別）

檢查改動的程式碼是否與**改動檔案內**或**同模組鄰近檔案**的既有風格一致：
- 引號風格（' vs "）
- type hints（是否一律加）
- 命名慣例（snake_case / camelCase）
- 縮排與換行慣例
- 註解語言與密度

判定規則：
- 改動引入的不一致 → R-style medium（建議改回）
- 既有已存在的不一致 → 不報告（非本次任務範圍）

**與 Q3 Surgical 邊界**（DR-5 修正）：Q3 Surgical 從 implementer 端要求「style match 既有」（自律）；R-style 從 reviewer 端反向兜底（檢查實際是否落實）——雙向覆蓋同一概念，互不重疊。
```

**行數限制**：≤ 17 行（code-reviewer.md）+ ≤ 3 行（產出物格式.md R-x 編號表新增 R-style 行）

**驗收**：grep `R-style` 在 code-reviewer.md ≥ 2 命中（heading + 條目）+ 產出物格式.md R-x 編號表含 R-style

---

### BC-5 [testable]：Goal 轉換範式（K-08）

**位置**：
1. `.claudedocs/agents/architect.md` 在 Step 1（需求理解）與 Step 2（BC-x 設計）之間插入 **Step 1.5：指令轉換**
2. `CLAUDE_TEMPLATE.md` 精簡閉環步驟 1 補一句「先做指令轉換」

**內容**（architect.md Step 1.5）：
```
**步驟 1.5 — 把命令式指令轉成可驗證目標（v6.1.0 新增 · Karpathy 學習）**

用戶指令通常是命令式（「加驗證」「修 bug」「優化效能」）。
進入 BC-x 設計前，先做轉換：

| 命令式 | 可驗證目標 |
|--------|----------|
| 加驗證 | 對 [X] 類無效輸入，寫測試讓它失敗，再讓測試通過 |
| 修 bug | 寫測試重現 bug，再讓測試通過 |
| 優化效能 | 量測當前 [指標]，目標達到 [數值]，並有 benchmark 可重跑 |
| 重構 X | 重構前後測試一致通過，且 [可量化改善指標] |

轉換後的可驗證目標就是 BC-x 的草稿。
```

**行數限制**：≤ 15 行（architect.md）+ ≤ 2 行（CLAUDE_TEMPLATE 步驟 1 補句）

**驗收**：grep `Step 1.5` 或 `指令轉換` 在 architect.md ≥ 1 命中 + CLAUDE_TEMPLATE 步驟 1 含對應補句

---

### BC-6 [testable]：Anti-Patterns Summary 對照表（K-16）

**位置**：`.claudedocs/concepts/閉環核心理念.md` 末尾新增「Anti-Patterns Summary」段（位於「主動質疑」段之後、「最後修訂」之前）

**內容**：
```
## Anti-Patterns Summary（v6.1.0 新增 · 仿 Karpathy EXAMPLES.md）

| 原則 | Anti-Pattern | 修正 |
|------|------------|------|
| Q1 Think | 默默選一種解讀就動手 | 列假設、有歧義列選項、必要時 push back |
| Q2 Simplicity | Strategy pattern 處理單一計算 | 一個函式直到複雜度真的需要 |
| Q3 Surgical | 修 bug 順手 reformat 引號 / 加 type hints | 只改解 bug 的那幾行 |
| Q4 Goal | 「我先 review 再改善」 | 「寫測試重現 bug → 讓它通過 → 確認無回歸」 |

詳見 CLAUDE_TEMPLATE Section 0 對映表。
```

**行數限制**：≤ 12 行

**驗收**：grep `Anti-Patterns Summary` 在閉環核心理念.md 命中 1 次

---

## 3 錯誤處理（EH-x）

### EH-1 [testable]：CLAUDE_TEMPLATE 行數超預算（DR-4 寬容區間 + DR-3 實作梯度 walk）

**預算計算**：
- v6.0.0 末態 510 行
- v6.1.0 改 CLAUDE_TEMPLATE：BC-1 步驟 1 自檢 +1 行 / BC-3 Section 11.5 +12 行（含 DR-1 性質標註 +1 行）/ BC-5 步驟 1 補句 +2 行 = **+15 行（寬容區間 +14~+22 行**——BC-1 第 7 問 1-2 行 / BC-3 含 heading 11-14 行 / BC-5 補句 1-2 行 / 排版自由度 ~3 行）
- v6.1.0 預期：**≤ 525 行（樂觀）/ ≤ 532 行（pessimistic 22 行膨脹）**

**條件 + 處理**：
- ≤ 525 → 通過（樂觀預期）
- 525 < 行數 ≤ 540 → R-x medium（含寬容區間 532 上限）
- > 540 → R-x **high** 回步驟 2

**實作梯度 walk 規則**（DR-3 修正 — 補上 v6.0.0「DR 修正傳遞性」教訓的預算傳遞缺口）：
implementer 每完成一個 BC 後跑 `wc -l dev-closed-loop/CLAUDE_TEMPLATE.md` 並重估剩餘預算；若中段已超 530 → 立即回報主 agent 評估是否需要瘦身既有段落。

### EH-2 [testable]：dependency 連動檔案漏改（v6.0.0 教訓「DR 修正傳遞性」）

**強制 walk** 6 條 K-x 連動檔案矩陣：

| BC-x | K-x | 連動檔案數 | 必動 |
|------|-----|-----------|------|
| BC-1 | K-02 | 3 | CLAUDE_TEMPLATE + architect.md + design-reviewer.md |
| BC-2 | K-03 | 1 | architect.md |
| BC-3 | K-05 | 2 | CLAUDE_TEMPLATE + code-reviewer.md |
| BC-4 | K-06 | 2 | code-reviewer.md + 產出物格式.md |
| BC-5 | K-08 | 2 | architect.md + CLAUDE_TEMPLATE |
| BC-6 | K-16 | 1 | 閉環核心理念.md |
| — | Skill 層 | 0 | `skill/init-claude.md` **no-op**（DR-2 修正：v6.1.0 不動 Skill 部署層；明標避免 implementer 漏想） |

任一連動漏改 → P3 R-x **high** 斷點 → 回步驟 2

### EH-3 [testable]：placeholder / fence 完整性（沿用 v6.0.0 EH-3）

部署後 grep `{{` / `}}` 仍剩或 fence 不平衡 → R-x high

---

## 4 驗證層級總表

| 設計項 | 驗證層級 | 驗證方式 |
|-------|---------|---------|
| BC-1 ~ BC-6 | [testable] | grep / wc 驗證 |
| EH-1 ~ EH-3 | [testable] | wc -l 行數 / grep 連動驗證 |

---

## 5 設計自檢 6 問（精簡閉環必做）

| 問題 | 結果 |
|------|------|
| ① 更簡單做法？ | ✅ 6 條 K-x 都是「補一條規則」，無更簡單替代 |
| ② 影響不相關模組？ | ✅ 6 個檔案 unique，BC 之間不互相影響 |
| ③ 邊界條件覆蓋？ | ✅ EH-1 / EH-2 / EH-3 涵蓋預算 / 連動 / placeholder |
| ④ 操作流程變複雜？ | ⚠️ 設計階段加 2 條自檢（BC-1 第 7 問 + BC-5 Step 1.5），整體流程加長 < 5%——可接受 |
| ⑤ 解法複雜度成正比？ | ✅ minor 升級 + 6 條小幅補強，比例對 |
| ⑥ 架構地基穩？ | ✅ v6.0.0 已對 6 條 K-x 留好錨點 |

無項目觸發 AskUserQuestion。

---

## 6 ⛔ 閘門檢查

- [✅] **學習查詢已執行**（問題追蹤 #003 + learning-log 4 條已標示）
- [✅] **字面證據掃描**：不適用
- [✅] **共用值檢測**：不適用
- [✅] **架構體質拆解**：v6.0.0 已做，本次延續
- [✅] **設計自檢 6 問**通過
- [✅] **BC-x ≥ 1**（共 6 條）
- [✅] **EH-x 符合領域預設**（純文檔升級，3 個 EH-x 涵蓋）
- [✅] **驗證層級已標注**（全 testable）
- [✅] **分層結構已聲明**（同 v6.0.0 三層）
- [✅] **DR 修正傳遞性教訓已納入**（EH-2 強制 walk 連動檔案）

**閘門狀態**：全部 ✅ → P1 完成，可進步驟 1b 設計快審。

---

## 7 學習查詢標示

**學習查詢**：問題追蹤命中 [#003] 已沿用 v6.0.0「Karpathy 原文 vs 延伸」分類緩解；[#004, #005] 不適用 / learning-log 命中 4 條 v6.0.0 事件 + 2 觀察項，本 P1 已將「DR 修正傳遞性」教訓納入 EH-2 強制 walk 連動檔案矩陣

---

## 8 影響檔案清單（給步驟 2 implementer）

### 執行順序

| Step | BC | K-x | 主檔案 | 連動檔案 |
|------|-----|-----|--------|---------|
| 1 | BC-3 | K-05 | `CLAUDE_TEMPLATE.md`（Section 11.5） | `code-reviewer.md`（pre-existing dead code 不算 R-x） |
| 2 | BC-4 | K-06 | `code-reviewer.md`（R-style 子類別） | `產出物格式.md`（R-x 編號表補 R-style） |
| 3 | BC-1 | K-02 | `CLAUDE_TEMPLATE.md`（步驟 1 第 7 問） | `architect.md`（Step 8）+ `design-reviewer.md`（步驟 2） |
| 4 | BC-2 | K-03 | `architect.md`（Step 1 行數估算 + 3x rule 段） | — |
| 5 | BC-5 | K-08 | `architect.md`（Step 1.5 指令轉換） | `CLAUDE_TEMPLATE.md`（步驟 1 補句） |
| 6 | BC-6 | K-16 | `concepts/閉環核心理念.md`（Anti-Patterns Summary） | — |

### 唯一檔案集合（步驟 3 一致性審查驗收用）

1. `dev-closed-loop/CLAUDE_TEMPLATE.md`（BC-1 / BC-3 / BC-5）
2. `dev-closed-loop/.claudedocs/agents/architect.md`（BC-1 / BC-2 / BC-5）
3. `dev-closed-loop/.claudedocs/agents/design-reviewer.md`（BC-1）
4. `dev-closed-loop/.claudedocs/agents/code-reviewer.md`（BC-3 / BC-4）
5. `dev-closed-loop/.claudedocs/standards/產出物格式.md`（BC-4）
6. `dev-closed-loop/.claudedocs/concepts/閉環核心理念.md`（BC-6）

= **6 個檔案**

### implementer 提示（DR-6 修正：step 編號精確性）

每個 agent 檔案改之前先 `Read` 該檔對應 Step / 段落確認當前 step 編號位置（避免靠記憶或 P1 spec 標的「Step 8」「步驟 2」與 agent 實際 step 不對齊）：
- BC-1 改 architect.md Step 8 之前 → 先 Read architect.md 找實際 Step 8 line
- BC-1 改 design-reviewer.md 步驟 2 之前 → 先 Read design-reviewer.md 找步驟 2 line
- BC-2 改 architect.md Step 1 之前 → 先 Read 找 Step 1 line（為 3x rule 加實作行數估算）
- BC-5 改 architect.md Step 1.5 插入點 → 先 Read 確認 Step 1 / Step 2 邊界

---

最後修訂：2026-04-26（精簡閉環 v6.1.0 步驟 1 P1 規格 **v2**，採用全 6 條 DR 修正後定稿：DR-1 性質標註 / DR-2 Skill 層 no-op / DR-3 實作梯度 walk / DR-4 寬容區間 / DR-5 R-style vs Q3 邊界 / DR-6 step 編號精確性。閘門全 ✅，可進步驟 2）
