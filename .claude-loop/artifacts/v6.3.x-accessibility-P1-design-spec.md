# v6.3.x Accessibility 補強 — P1 設計規格（精簡閉環步驟 1 · v2 — 採納 P1b 修正）

> 日期：2026-04-26
> Branch：main（不開新 feature 分支，這是 patch 級別）
> 上游：/sc:analyze + /sc:reflect 結論：方法論評分 76.9/100 (B 偏 B+)，目標推到 80+
> 用戶決策：路徑 A — 做 Top 3（觀察性 / 採納門檻 / 可學習性）+ 預期 +4.2 → 81.1
> Scope：v6.3.x patch（無新 K-x，純對外 accessibility 補強，不 version bump）

---

## 0 學習查詢結果

| 來源 | 結果 |
|------|------|
| 問題追蹤長期警惕模式（6 條） | 命中 [#006] 行數預算估算樂觀（每檔單獨估、reviewer 主動 wc -l）/ [#001-#005] 不適用 |
| `.claude-loop/learning-log.md`（v6.0~v6.3） | 命中 v6.2.0 R-1（行數超出）+ 「DR 修正傳遞性」觀察項已正式結束（v6.3.0 milestone closure）+ session 內 CLAUDE.md path 跨平台教訓（macOS 硬編 → 跨 OS 描述）|

**關鍵教訓帶過來**：
- 嚴格沿用 #006：每個新檔 / 修改檔單獨估行數 + 緩衝 ≥ 5 行 + reviewer 主動 wc -l
- 路徑硬編教訓：QUICKSTART.md / README.en.md 不寫 OS-specific 路徑
- 內容對外導向（沿剛 README v6.x 重寫教訓）：避免內部編號 K-x / BC-x / DR-x 暴露給外部讀者

**Step 0a / 0b / 0c**：不適用
**v6.0.0 教訓 #1（部署生命週期視角）**：不適用（無 metadata grep 字串改動）

---

## 1 分層結構聲明

純文檔擴充，分三層：
- **內部觀察層**：`concepts/方法論運作指標.md` 加 baseline 附錄（內部讀者）
- **對外讀者層**：`README.en.md`（國際讀者新檔）+ 根 `README.md` 補充（中文讀者）
- **學習者層**：`dev-closed-loop/QUICKSTART.md`（30 分鐘版新檔）

不動：CLAUDE_TEMPLATE / .claudedocs/agents / hooks / setup.sh / migration-notes

---

## 2 邊界條件（BC-x）

### BC-1 [testable]：KPI baseline 附錄（觀察性 7 → 8.5）

**位置**：`dev-closed-loop/.claudedocs/concepts/方法論運作指標.md` 末尾「最後修訂」之前

**內容大綱**：
- 表格：v6.0/v6.1/v6.2/v6.3 4 個閉環的 DR-x high / R-x high / 升格次數 / 健康判定
- 簡短分析（樣本太小無法判定門檻是否合理，需累積 ≥ 5 個閉環再校準）
- 註：4 個 milestone = 1 個月（2026-04-26 集中），不對應「升格 / 月」健康區間（需 ≥ 6 個月才 valid）

**行數限制**（v2 修正：採 DR-1 high）：≤ 20 行（91 → ≤ 111，緩衝 9 行 ✅）

**驗收 BC-K-A1**：
- grep `## v6.x 系列實測 baseline` 在檔案命中 1
- 表格含 4 行（v6.0~v6.3）× 4 欄（DR-x / R-x / 升格 / 健康）
- 行數 ≤ 116（嚴守 #006 預防做法 c）

### BC-2 [testable]：對外 README 強化（採納門檻 5.5 → 7）

**位置 a**：根 `README.md` 開頭 / 安裝指令前
- 加「定位聲明」段（≤ 5 行）：「個人 hardcore 修煉工具」+ 微小直通保護指引

**位置 b**：根 `README.md` 開頭加多語言連結（≤ 2 行）：
- `English version: README.en.md`
- `30-min quick start: dev-closed-loop/QUICKSTART.md`

**位置 c**：新檔 `README.en.md`（根目錄）
- 60-80 行純英文版
- 結構：what / why / install / use / methodology overview / link to Chinese
- **翻譯約束**（v2 修正：採 DR-3 medium）：三段任務分流避免 literal translation；用業界慣例詞「small/medium/large task」對應微小/中型/大型；avoid 直譯 BC-x / EH-x（用 "design contract IDs"）

**行數限制**（v2 修正：採 DR-1 high #006 結構化區塊下限 ≥ 15-20 行）：
- 根 README：197 → ≤ 230（+27 行：5 定位 + 2 連結 + 20 微小直通保護段）緩衝 6 行 ✅
- README.en.md：60-80 行新檔

**驗收 BC-K-A2**：
- grep `個人 hardcore` 或「個人 .* 工具」在根 README 命中 1
- grep `README.en.md` 在根 README 命中 ≥ 1
- grep `QUICKSTART` 在根 README 命中 ≥ 1
- README.en.md 存在且行數在 60-80 範圍
- 微小直通保護段 explicit 標出（grep `< 50 行` 或「微小任務」醒目段）

### BC-3 [testable]：QUICKSTART.md 新檔（可學習性 6.5 → 7.5）

**位置**：`dev-closed-loop/QUICKSTART.md`（新檔，跟 dev-closed-loop/README.md 並列）

**內容大綱**（5 + 3 + 連結結構）：
1. 5 個核心概念（每個 ≤ 3 句）：
   - 閉環五階段（架構 → 實作 → 檢核 → 測試 → 自證）
   - BC-x / EH-x（設計編號讓所有階段 reference 同一個東西）
   - 三段任務分流（微小直通 / 中型精簡六步 / 大型完整 5-Phase）
   - 認知驗證（Claude 對自己 + 對用戶事實前提都會質疑）
   - 學習自動沉澱（learning-log → 升格警惕模式）
2. 3 個常見場景示例（each ≤ 10 行）：
   - 修一個 typo → 微小直通
   - 加一個函式 → 中型精簡六步
   - 設計新模組 → 大型完整 5-Phase
3. 進階指南連結：
   - 完整方法論：`.claudedocs/`
   - 設計歷史：`design/`
   - KPI 機制：`.claudedocs/concepts/方法論運作指標.md`

**行數限制**：80-120 行新檔（沿 K-07 範例庫 / K-11 KPI 檔的範圍）

**驗收 BC-K-A3**：
- 檔案存在於 `dev-closed-loop/QUICKSTART.md`
- 行數 80-120
- 5 個概念 heading 全部命中
- 3 個場景示例 heading 全部命中
- 進階指南連結 ≥ 3 個

**排除原則**（v2 修正：採 DR-2 medium，避免跟 dev-closed-loop/README 重疊）：
- QUICKSTART.md 走 **use-case 視角**（給「想學會用」的人）：5 概念 + 3 場景一條 happy-path
- dev-closed-loop/README.md 走 **reference 視角**（給「想找東西」的人）：文件結構索引 + 安裝細節
- 字串相似度驗收：grep `文件結構` `目錄結構` `Skill 部署細節` 在 QUICKSTART 命中為 0（這些屬 reference 視角範疇）

---

## 3 EH-x

純文檔擴充，無外部 I/O，**聲明跳過**。

---

## 4 IF-x / CR-x

無模組間介面變更。新檔（README.en.md / QUICKSTART.md）跟既有檔案的關係只是「被 README 引用」，不是 import。

---

## 5 設計決策

| 決策 | 選擇 | 排除選項 | 理由 |
|------|------|---------|------|
| 版本號 | **不 bump**（patch 級別） | v6.4.0 milestone | 無新 K-x 概念、純對外擴充；v6.x 已正式收尾，不為 accessibility 補強再開 milestone |
| 是否寫 design/12 | **不寫** | 寫 design/12-accessibility-patch.md | 沿 v6.x 規範，design 用於有概念性決策；本次純文檔 polish |
| 觀察性附錄位置 | 加在既有 `方法論運作指標.md` 末尾 | 開新檔 `concepts/baseline-history.md` | 內容跟 K-11 高度耦合（baseline 是 KPI 的歷史數據），分檔反而割裂 |
| 英文版範圍 | 只根 `README.en.md` | 全套 dev-closed-loop/.claudedocs/ 翻譯 | 採納門檻優化第一步是「能 grasp」，不是「能深讀」；深度閱讀者可學中文或讀代碼 |
| QUICKSTART 位置 | `dev-closed-loop/QUICKSTART.md`（跟方法論主體並列） | 根 `QUICKSTART.md` | 方法論本體在 dev-closed-loop/，QUICKSTART 是其一部分；根目錄留給對外宣傳檔 |
| migration-notes-v6.4 | **不加** | 加 inline 4 行 | patch 級無 breaking 也無 extension 需要部署目標升級 |

---

## 6 行數預算（v2 修正：DR-1 high 採納 — 全部緩衝 ≥ 5）

| 檔案 | 當前 | 上限 | 增量 | 緩衝 |
|------|------|------|------|------|
| `dev-closed-loop/.claudedocs/concepts/方法論運作指標.md` | 91 | ≤ 111 | +20（baseline 附錄壓 ≤ 20）| **9 行 ✅** |
| 根 `README.md` | 197 | ≤ 230 | +27（5 定位 + 2 連結 + 20 微小直通段，#006 結構化下限）| **6 行 ✅** |
| `README.en.md`（新） | — | 80 | 60-80 行 | 20 行 buffer ✅ |
| `dev-closed-loop/QUICKSTART.md`（新） | — | 120 | 80-120 行 | 40 行 buffer ✅ |
| 跨檔合計 | — | — | +187 ~ +247 行 | — |

**v2 修正紀錄**（沿 #006 預防做法）：
- 微小直通段從 11 行 → **20 行**（#006 (a)：結構化區塊單獨估 ≥ 15-20 行）
- 根 README 上限從 215 → **230**（緩衝從 0 → 6）
- 方法論運作指標.md 上限從 116 → **111**（附錄上限從 25 → 20，緩衝從 4 → 9）

---

## 7 設計自檢（7 問 · K-02）

| # | 自檢題 | 答 |
|---|-------|-----|
| ① | 更簡單的做法？ | 已挑 Top 3（最高 ROI），其他改善（拆 CHANGELOG / 補語言指南）ROI 更低 |
| ② | 影響不相關模組？ | 不影響 CLAUDE_TEMPLATE / agents / hooks / setup.sh；只動對外文檔層 + concepts 層 |
| ③ | 邊界條件覆蓋？ | 3 條 BC-K-A1/A2/A3 |
| ④ | 操作流程變複雜？ | 完全相反——簡化外部讀者 onboarding |
| ⑤ | 解法複雜度成正比？ | 是（patch 級工程量對應 patch 級設計）|
| ⑥ | 架構地基穩？ | 是，沿用既有 K-11 / README / 方法論結構 |
| ⑦ | 資深工程師看到會說過度設計嗎？（K-02）| **無風險**：3 個檔案各自單一目的，無新概念，無新依賴 |

---

## 8 風險清單

| ID | 風險 | 嚴重度 | 緩解 |
|----|------|--------|------|
| RISK-1 | 根 README 預算緩衝 0 | 中 | 上限放寬到 220 + 實作時用「醒目段」設計避免 verbose |
| RISK-2 | 方法論運作指標.md 預算緩衝 4 < 5（#006 硬規則）| 低 | 實作時附錄壓 ≤ 20 行（緩衝 9）|
| RISK-3 | README.en.md 翻譯品質（我是 LLM 不是 native）| 低 | 用 GitHub repo 通用慣例語（install / use / learn more）+ 簡潔，避免 idiom |
| RISK-4 | QUICKSTART 跟既有 dev-closed-loop/README 內容重疊 | 中 | QUICKSTART 是「30 分鐘上手」走 use-case 視角；既有 README 是「文件結構索引」走 reference 視角；分工清楚 |
| RISK-5 | 內部編號（K-x / BC-x）誤入對外文檔 | 中 | 對外文檔（README.en.md / QUICKSTART）explicit 不用內部編號；CLAUDE.md / 設計層保留 |
| RISK-6 | 預期分數提升（+4.2）可能高估 | 低 | 即使只 +2 仍能往 79 推進；80+ 不達也是 B+ 上限 |

---

## 9 學習查詢結果（最終標示）

**學習查詢**：問題追蹤命中 [#006] / learning-log 命中 v6.2.0 R-1 + 跨平台路徑教訓。詳見第 0 節。

---

最後修訂：2026-04-26（v6.3.x P1 v2 — 採 P1b DR-1 high 全部緩衝 ≥ 5、DR-2/3 medium 排除原則 + 翻譯約束、DR-4 low 第 9 節壓三句；進步驟 2 實作）
