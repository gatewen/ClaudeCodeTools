# 閉環學習日誌

---

## [2026-04-05] S3 熔斷修改無效 — 改了沒被呼叫的函式 `[circuit-breaker]` `[code-reviewer]`

### 事件

用戶要求：圖片生成失敗 3 次後停止 pipeline，不再產出 placeholder 藍底文字卡。

我修改了 `s3_visual.rs` 的 `execute_s3` 函式，移除 placeholder 降級邏輯，改為 return error。**編譯成功，告知用戶已完成。但實際執行時 placeholder 仍然出現、pipeline 仍然跑完。**

用戶發現問題後，經調查：`executor.rs` 有自己獨立的 S3 執行邏輯（第 540-823 行），完全沒有呼叫 `s3_visual.rs` 的 `execute_s3`。**我修改的函式根本不在執行路徑上。**

### 失敗根因

1. **假設唯一路徑** — 看到 `s3_visual.rs` 有 `execute_s3` 函式就認定它是唯一的 S3 實作，沒驗證
2. **沒追呼叫鏈** — 沒有 `grep -r "execute_s3"` 確認誰在呼叫這個函式
3. **沒搜尋替代實作** — 沒在 `executor.rs` 搜尋 `S3`、`placeholder`、`image_provider` 等關鍵字
4. **沒做行為驗證** — 改完直接說「完成」，沒實際觸發失敗場景確認 pipeline 是否真的停止
5. **信任編譯成功 = 功能正確** — 編譯通過不代表改對了地方

### 影響

- 用戶重新生成影片兩次，浪費 API 費用和時間
- 產出的影片仍有藍底 placeholder 畫面
- 用戶信任度受損

### 教訓 → 閉環方法論修正建議

#### 修正 1：因果鏈分析（Section 9）加入「呼叫鏈追蹤」

現行的因果鏈分析只看「引用此檔案的模組」，但沒有反向確認「這個函式是否真的在執行路徑上」。

**建議新增閘門規則：**

```
修改函式 X 前，因果鏈分析必須包含：
├─ 呼叫者追蹤：grep -r "函式名" 確認所有呼叫者
│   ├─ 呼叫者 = 0 → ⛔ 禁止修改，先找真正的執行路徑
│   ├─ 呼叫者 ≥ 1 → 列出所有呼叫者，確認影響範圍
│   └─ 特別注意：executor/orchestrator 層可能繞過模組自己 inline 實作
├─ 替代路徑搜尋（pipeline stage 修改時必做）：
│   └─ 在 executor/orchestrator/runner 搜尋該 stage 的處理邏輯
└─ 確認修改點在實際執行路徑上後，才進行修改
```

#### 修正 2：測試驗證（Phase 4 / 步驟 4）加入「行為驗證」

現行的測試驗證只跑 `cargo test`，但單元測試不覆蓋 executor 層的整合行為。

**建議新增驗證步驟：**

```
修改「錯誤處理/熔斷/降級」邏輯時，除了 cargo test 外必須：
1. 確認修改點的輸出目錄：是否還有不預期的檔案（如 placeholder）
2. 若可行，實際觸發失敗場景驗證行為
3. 確認錯誤是否被上層 catch 後靜默繼續（穿透驗證）
```

#### 修正 3：自証（Phase 5 / 步驟 4.5）加入「死碼偵測」

如果修改一個函式後，該函式的呼叫者為 0（或只有測試呼叫），應在自証階段標記警告。

**建議新增檢查：**

```
自証時確認：
- 被修改的函式是否有非測試的呼叫者？
- 如果被修改的函式在 cargo build 中產生 dead_code warning → 紅旗
```

### 反模式速查表

| 反模式 | 正確做法 | 偵測方式 |
|--------|---------|---------|
| 看到函式名就改 | 先追呼叫鏈，確認在執行路徑上 | `grep -r "函式名"` |
| 改一個檔案就收工 | 搜尋所有替代實作路徑 | 搜尋 executor/orchestrator 層 |
| 編譯通過 = 改對了 | 必須觸發目標場景做行為驗證 | 實際執行 + 檢查輸出 |
| 信任模組分層 | executor 可能繞過模組自己實作 | 搜尋上層的 inline stage 邏輯 |
| 單元測試通過 = 整合正確 | 錯誤處理需要穿透驗證 | 追蹤 error 在呼叫鏈中的傳播 |

### 適用場景

- 任何修改「Stage 執行邏輯」的任務
- 任何修改「錯誤處理/降級/fallback」的任務
- 任何「我改了 X 但行為沒變」的 debug 場景
- 大型專案中模組函式與 orchestrator 層可能有重複實作的情況
